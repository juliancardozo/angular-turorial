 # Calculator

  This is a simple `Calculator` class that provides basic arithmetic operations and some additional mathematical functions.

  ## Methods

  ### Arithmetic Operations

  - `static add(a: number, b: number): number`
    - Adds two numbers together.
    - **Parameters:**
      - `a`: The first number.
      - `b`: The second number.
    - **Returns:** The sum of the two numbers.

  - `static subtract(a: number, b: number): number`
    - Subtracts the second number from the first number.
    - **Parameters:**
      - `a`: The first number.
      - `b`: The second number.
    - **Returns:** The difference